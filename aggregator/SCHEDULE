

Week of 27 Jan 2014
 o Python program for periodically waking up and querying data
 o Class of data fetcher threads 
   - First Implement key-value fetcher thread that wakes up every so often and queries
   - Each thread stores the most recent timestamp to enable REST since parameter (critical part in ensuring non duplicate data
   - Can start with everything as a key-value?
   - Dummy json provider (maybe server), python program (import json)


Consider using cron jobs for inserting data instead of sleeping threads 
https://help.ubuntu.com/community/CronHowto